{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.shift_jump": {
      "initial_state": "default",
      "states": {
        "default": {
          "transitions": [
            {
              "shadow_use3": "(query.get_equipped_item_name == 'shadow') && (query.is_sneaking) && (query.is_jumping)"
            },
            {
              "bluf_use3": "(query.get_equipped_item_name == 'bluf') && (query.is_sneaking) && (query.is_jumping)"
            },
            {
              "darkn_use3": "(query.get_equipped_item_name == 'darkn') && (query.is_sneaking) && (query.is_jumping)"
            },
            {
              "ice_use3": "(query.get_equipped_item_name == 'ice') && (query.is_sneaking) && (query.is_jumping)"
            }
          ],
          "on_exit": [
            "/tag @p remove custom"
          ]
        },
        "shadow_use3": {
          "transitions": [
            {
              "default": "(query.all_animations_finished)"
            }
          ],
          "animations": [
            "shadow_finished3"
          ]
        },
        "bluf_use3": {
          "transitions": [
            {
              "default": "(query.all_animations_finished)"
            }
          ],
          "animations": [
            "bluf_finished3"
          ]
        },
        "darkn_use3": {
          "transitions": [
            {
              "default": "(query.all_animations_finished)"
            }
          ],
          "animations": [
            "darkn_finished3"
          ]
        },
        "ice_use3": {
          "transitions": [
            {
              "default": "(query.all_animations_finished)"
            }
          ],
          "animations": [
            "ice_finished3"
          ]
        }
      }
    }
  }
}