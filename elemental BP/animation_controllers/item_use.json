{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.items.use": {
      "initial_state": "default",
      "states": {
        "default": {
          "transitions": [
            {
              "shadow_use": "(query.get_equipped_item_name == 'shadow') && (!query.is_sneaking) && (query.is_using_item)"
            },
            {
              "bluf_use": "(query.get_equipped_item_name == 'bluf') && (!query.is_sneaking) && (query.is_using_item)"
            },
            {
              "darkn_use": "(query.get_equipped_item_name == 'darkn') && (!query.is_sneaking) && (query.is_using_item)"
            },
            {
              "ice_use": "(query.get_equipped_item_name == 'ice') && (!query.is_sneaking) && (query.is_using_item)"
            },
            {
              "soul_use": "(query.get_equipped_item_name == 'soul') && (!query.is_sneaking) && (query.is_using_item)"
            }
          ],
          "on_exit": [
            "/tag @p remove custom"
          ]
        },
        "shadow_use": {
          "transitions": [
            {
              "default": "(query.all_animations_finished)"
            }
          ],
          "animations": [
            "shadow_finished"
          ]
        },
        "bluf_use": {
          "transitions": [
            {
              "default": "(query.all_animations_finished)"
            }
          ],
          "animations": [
            "bluf_finished"
          ]
        },
        "darkn_use": {
          "transitions": [
            {
              "default": "(query.all_animations_finished)"
            }
          ],
          "animations": [
            "darkn_finished"
          ]
        },
        "ice_use": {
          "transitions": [
            {
              "default": "(query.all_animations_finished)"
            }
          ],
          "animations": [
            "ice_finished"
          ]
        },
        "soul_use": {
          "transitions": [
            {
              "default": "(query.all_animations_finished)"
            }
          ],
          "animations": [
            "soul_finished"
          ]
        }
      }
    }
  }
}